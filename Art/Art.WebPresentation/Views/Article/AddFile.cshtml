@*@model PictureViewModel

@{
    ViewData["Title"] = "AddFile";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>AddFile</h1>

<h4>PictureViewModel</h4>

    <div class="col-md-3">
        <form asp-action="AddFile" asp-controller="Article" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label for="exampleInputName">Имя файла</label>
                <input type="text" class="form-control" id="exampleInputName" placeholder="Имя" asp-for="FullName">
            </div>
            <div class="form-group">
                <label for="FormControlFile"> Выберите файл</label>
                <input type="file" class="form-control-file" id="FormControlFile" asp-for="FormImage">
            </div>
            <div class="form-group">
                <input type="submit" value="Загрузить" class="btn btn-primary" />
            </div>
        </form>
    </div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}*@
